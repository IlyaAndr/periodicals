= simple_form_for @kind, :html => {:class => 'well form-horizontal' } do |f|
  = f.input :name
  .form-actions
    = f.submit 'Save', class: 'btn btn-success'
    = f.submit "Reset", type: :reset, class: 'btn btn-inverse'
